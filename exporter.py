#! /usr/bin/env python3



	def revision(self, weights_revision, ignoring, weights_ignoring):
		self.weights_revision = weights_revision
		self.ignoring = ignoring
		self.weights_ignoring = weights_ignoring


	def experiment_design((self, basic, advanced, secondary_criterion_cost):
		self.basic = basic
		self.advanced = advanced
		self.secondary_criterion_cost = secondary_criterion_cost
